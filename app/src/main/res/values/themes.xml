<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.PixelPulse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/pixel_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/pixel_blue_light</item>
        <item name="colorOnPrimaryContainer">@color/pixel_blue_dark</item>
        
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/pixel_green</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/pixel_green_light</item>
        <item name="colorOnSecondaryContainer">@color/pixel_green_dark</item>
        
        <!-- Background colors -->
        <item name="colorSurface">@color/surface_light</item>
        <item name="colorOnSurface">@color/on_surface_light</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorOnBackground">@color/on_background_light</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        
        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
    
    <!-- Splash screen theme -->
    <style name="Theme.PixelPulse.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pixel_blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/pixel_blue</item>
        <item name="windowSplashScreenBrandingImage">@drawable/ic_pixel_pulse_text</item>
        <item name="postSplashScreenTheme">@style/Theme.PixelPulse</item>
    </style>
</resources> 